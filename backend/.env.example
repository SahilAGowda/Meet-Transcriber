# Model is downloaded automatically by faster-whisper on first use.
WHISPER_MODEL=base.en
WHISPER_COMPUTE_TYPE=int8
AUDIO_CHUNK_SECONDS=1.5
# ffmpeg must be installed and available in PATH.
FFMPEG_BIN=ffmpeg

